[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = strict

markers =
    unit: Isolated mathematical and algorithm engine tests.
    integration: High-volume concurrent and end-to-end data pipeline tests.
